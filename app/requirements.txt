fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
httpx>=0.27
python-multipart>=0.0.9
cryptography>=42.0  # encryption at rest for OAuth client secrets/tokens -- see toci/crypto.py
app-store-server-library>=3.0  # Apple's official library -- JWT auth to the App Store
# Server API + JWS signature verification against Apple's real root CA. See
# toci/apple_iap.py and docs/app-store-setup.md.
pytest>=8.0  # dev dependency -- tests/ run against an isolated throwaway SQLite file, never toci.db
